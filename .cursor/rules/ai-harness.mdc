---
description: 
alwaysApply: true
---

# AI Harness 地图

本仓库采用 **harness 工程**：Rules 约束、Skills 编排、Gates 验证、Git/CI 强制执行。

## 目录职责

| 路径 | 用途 |
|---|---|
| `.cursor/rules/` | 自动注入的约束（短、硬） |
| `.cursor/skills/` | 任务入口（何时做 + 步骤摘要） |
| `.ai/workflows/` | 详细分步流程 |
| `.ai/gates/` | 完成前的检查清单 |
| `.ai/templates/` | 产出物格式模板 |
| `.ai/memory/` | 项目决策与失败日志 |
| `.ai/progress/` | **进度存档**（新会话入口 `CURRENT.md`，功能完成写 `log/`） |
| `.ai/planning/` | FEATURE-PLAN + **T1–T3** 变更落盘（见 `planning/README.md`） |
| `.ai/fixes/` | **T0** Bug 修复落盘 |
| `.cursor/mcp.json` | **MCP 服务器**（Context7、Playwright） |
| `.cursor/MCP.md` | MCP 用途、Agent 约定、故障排查 |

## 任务路由

| 用户意图 | 阅读 workflow | Skills | Gates |
|----|---|-----|----|
| **新会话 / 续作 / 进度** | — | `project-progress` | — |
| **功能规划 / roadmap / 做什么** | `.ai/planning/FEATURE-PLAN.md` | — | — |
| **任何新任务 / 改需求** | `.ai/planning/README.md` | — | `tier-classification` |
| **需求分析 / 方案设计** | `.ai/workflows/requirements-design.md` | `requirements-design` | `tier-classification`、`change-lite-review` 或 `requirements-review`+`design-review` |
| Git 提交 / 暂存变更 | — | `.cursor/skills/commit-workflow/` | `.ai/gates/code-quality.md` |
| 新功能 / 实现（方案已清） | `.ai/workflows/feature-development.md` | `feature-development` | `pre-implementation`、`code-quality` |
| UI 页面 / 布局 / 桌面壳 | `.ai/workflows/ui-page-development.md` | `ui-page-development`、`css-layout`、`scrollable-layout`、`macbook-ui`、`frontend-component` | `frontend-quality`、`ui-quality`、`code-quality` |
| 前端组件 / 页面逻辑拆分 | — | `frontend-component` | `frontend-quality` |
| Bug 修复（T0） | `.ai/workflows/bugfix.md` | `bugfix` | `tier-classification`、`code-quality` |
| 后端 Service / API | — | `backend-service` | `backend-quality`、`api-contract` |
| Harness / 工作流设计 | — | `harness-engineering` | — |
| 第三方库 API / 版本 / 文档 | — | **Context7 MCP** | — |
| UI 验证 / 浏览器复现 / SSE 展示 | — | **Playwright MCP** | `ui-quality` |
| **E2E 回归 / smoke 测试** | [`e2e/README.md`](../../e2e/README.md) | — | `ui-quality` |

## Agent 协议

0. **续作** — 新会话或用户说「继续项目」：先读 `.ai/progress/CURRENT.md`；做新功能前读 `.ai/planning/FEATURE-PLAN.md` 对齐范围。
1. **开始** — 匹配路由表；**先 `tier-classification`（Agent 提议 T0–T3，用户确认）** → T0 `bugfix` / T1–T3 `requirements-design` → `feature-development`；生命周期见 `.ai/workflows/agent-lifecycle.md`。
2. **实现** — 遵循所 touch 文件对应的 `.cursor/rules/*.mdc`；外部库 API 不确定时用 **Context7 MCP**（详见 `.cursor/MCP.md`）。
3. **验证** — 跑 gates（lint、build）；前端/UI 改动可用 **Playwright MCP** 在本地 dev 上复验；宣称完成前阅读 `.ai/gates/`。
4. **纠错** — hook/CI 失败时读 `.ai/memory/common-failures.md`；修复后重新验证。
5. **记忆** — 遇到新的失败模式，在 `common-failures.md` 追加一行。
6. **存档** — 功能完成并 commit 后：写 `.ai/progress/log/YYYY-MM-DD-*.md`，更新 `CURRENT.md`（见 `project-progress` skill）。

## 硬门禁（不可跳过）

- **GitHub Actions**（`.github/workflows/ci.yml`）：`pnpm lint` → `pnpm build` → `pnpm test:e2e:smoke`
- **pre-commit**：Husky → lint-staged → web/components ESLint；backend ESLint + `tsc`
- **ESLint AI Guardrails**（`eslint.ai-guardrails.mjs`，`--max-warnings=0`）：

| 范围 | 函数行数 | 文件行数 | 参数 | 类/文件 | 魔法数字 |
|---|----|----|---|---|----|
| `apps/backend` | ≤50 | ≤250 | ≤4 | 1 class | `src/services/**` |
| `apps/web` + `packages/components` | ≤80 | ≤250 | ≤4 | 1 class | `src/service/**` |

- **User rules**：禁止 `ts-ignore`、禁止随意 `any`、组件必须 TypeScript、请求需处理 loading/error/empty
- **Agent 不得自行放宽** ESLint guardrails 配置；需用户确认

## 项目 Skills 清单（`.cursor/skills/`）

| Skill | 用途 |
|----|---|
| `requirements-design` | 定级后 T1 lite / T2–T3 完整设计 |
| `bugfix` | T0 Bug 修复 |
| `feature-development` | 设计确认后的实现 |
| `ui-page-development` | 页面 / 桌面壳工作流入口 |
| `css-layout` | Flex 优先布局 |
| `scrollable-layout` | 页内滚动 / 虚拟列表 |
| `macbook-ui` | macOS 玻璃拟态视觉 |
| `frontend-component` | 组件 / hook 拆分 |
| `backend-service` | 后端 Service 编排 |
| `commit-workflow` | Git 提交 + lint-staged |
| `project-progress` | 进度存档 CURRENT / log |
| `harness-engineering` | 维护 harness 本身 |

## 全局 Skill 约定

- **UI 视觉**：只用项目 `macbook-ui` + `tokens.css`。**勿用**全局 `frontend-design`（风格冲突）。
- **LLM 集成**：本仓库 backend 用 OpenAI / LangChain。**勿用**全局 `claude-api`。
- **E2E**：优先仓库 `pnpm test:e2e:*` 与 Playwright MCP，勿混用无关 artifact / 文档类 skill。
- Cursor 内置 `~/.cursor/skills-cursor/` 与本项目无关时忽略（如 canvas、statusline、sdk）。
- 原个人 skill `scrollable-layout`、`macbook-ui` 已迁入项目，勿再依赖 `~/.cursor/skills/` 副本。
