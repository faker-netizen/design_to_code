# 功能开发

## 前置

- planning / fixes 已确认（`pre-implementation` gate）

## 步骤

1. 读 `02-solution-design.md` 或 `01-change-lite.md` 中的 AC
2. 按 `.cursor/rules/` 实现；小步提交
3. 跑 lint / build
4. `post-implementation` gate
5. 用户认可后：`project-progress` 写 log + 更新 CURRENT

Skill：`.cursor/skills/feature-development/SKILL.md`
